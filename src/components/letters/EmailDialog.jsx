import { useState } from "react";
export default function EmailDialog({ defaultEmail, onClose, onSend, loading }) { const [data,setData]=useState({recipient_email:defaultEmail||"",subject:"",message:""}); return <div className="letter-modal"><form className="letter-modal__box email" onSubmit={(e)=>{e.preventDefault();onSend(data)}}><h3>Send Letter</h3><input required type="email" placeholder="Recipient email" value={data.recipient_email} onChange={e=>setData({...data,recipient_email:e.target.value})}/><input required placeholder="Subject" value={data.subject} onChange={e=>setData({...data,subject:e.target.value})}/><textarea placeholder="Message" value={data.message} onChange={e=>setData({...data,message:e.target.value})}/><div><button type="button" onClick={onClose}>Cancel</button><button disabled={loading}>{loading?"Sending…":"Send email"}</button></div></form></div>; }
